class BankAcount {
    constructor(saldo) {
        this.saldo = 0
    }

    deposit(amount) {
        let = amount(parseFloat(prompt('Masukkan jumlah saldo')))
        try {
            if (amount(!isNaN)) {

            }
        } catch {

        }


    }

    withdraw(amount) {
        let = amount(parseFloat(prompt()))
    }
}
